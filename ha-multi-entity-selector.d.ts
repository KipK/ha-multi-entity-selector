// Type definitions for ha-multi-entity-selector

// Import and re-export all types from the module
import { MultiEntitySelector } from './dist/index';
export { MultiEntitySelector } from './dist/index';
export * from './dist/index';

// Export default for convenience
export default MultiEntitySelector;

// Note: Importing this module registers the custom element 'multi-entity-selector'
// You can use it in your HTML as <multi-entity-selector></multi-entity-selector>
// even if you don't directly reference the MultiEntitySelector class in your code.
